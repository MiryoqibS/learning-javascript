# 🔄 Тема 5.9 - Object.keys(), Object.values(), Object.entries()

---

## 🔹 Object.keys(obj)

Возвращает массив строк со всеми собственными перечисляемыми ключами объекта (игнорируются свойства в прототипе и символьные ключи)

```javascript
const user = { name: "Miryoqib", age: 14, city: "Tashkent" };
console.log(Object.keys(user)); // ["name", "age", "city"]
```

Это «реальный» массив, а не просто итератор

Порядок элементов соответствует порядку добавления свойств в объект

---

## 🔹 Object.values(obj)

Возвращает массив значений всех собственных перечисляемых свойств объекта, в том же порядке, что и `Object.keys`

```javascript
console.log(Object.values(user)); // ["Miryoqib", 14, "Tashkent"]
```

Полезно, когда нужно работать только со значениями без явного упоминания ключей

---

## 🔹 Object.entries(obj)

Возвращает массив пар [ключ, значение] для каждого собственного перечисляемого свойства объекта

```javascript
for (const [key, value] of Object.entries(user)) {
  console.log(`${key} → ${value}`);
}
// name → Miryoqib
// age → 14
// city → Tashkent
```

Отлично подходит для for…of и цепочек методов массива (`map`, `filter` и т.д.)

Итоговый массив — «реальный» массив массивов, а не итератор

---

## 🔄 Трансформация объекта

-Вызываем Object.entries(obj), чтобы получить массив пар [ключ, значение]
-Применяем методы массива (`map`, `filter` и т.д.) к этому массиву
-Используем Object.fromEntries(array), чтобы обратно превратить модифицированный массив пар в объект

```javascript
const prices = { apple: 1, banana: 2 };
const doubled = Object.fromEntries(
  Object.entries(prices).map(([k, v]) => [k, v * 2])
);
console.log(doubled); // { apple: 2, banana: 4 }
```

---

## 📌 Дополнительные замечания

-Символьные свойства игнорируются, как и у `for…in`
-Не работают с неперечисляемыми свойствами — они тоже игнорируются
-При работе со «псевдомассивами» (объектами с числовыми ключами) эти методы также применимы, но порядок соответствует строковой сортировке ключей

---

## 🧠 Личный вывод

Теперь я не использую громоздкие `for…in` + проверку `hasOwnProperty`, а вместо этого применяю `Object.keys`, `Object.values` и `Object.entries` для чистого и читаемого кода. Эти методы стали неотъемлемой частью моей работы с данными в JavaScript.
