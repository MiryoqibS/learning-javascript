# ✍️ Тема 3.2 — Стиль кода в JavaScript

## 🧠 Что такое стиль кода?

**Стиль кода** — это набор правил и рекомендаций по написанию кода, направленных на повышение его читаемости и поддерживаемости. Соблюдение единого стиля помогает команде работать с кодом эффективно и без недопонимания.

---

## 📐 Основные правила оформления кода

## 🔹 Отступы

---

- Используй **2 пробела** для каждого уровня вложенности.
- Не используй табуляцию (`\t`) для отступов.

```javascript
function greet() {
  console.log("Привет!");
}
```

## 🔹 Фигурные скобки

---

Открывающая скобка { ставится на той же строке, что и соответствующее ключевое слово.

Между ключевым словом и скобкой должен быть пробел.

```javascript
if (isActive) {
  doSomething();
}
```

## 🔹 Пробелы

---

Ставь пробелы после запятых и вокруг операторов.

```javascript
let sum = a + b;
```

## 🔹 Точки с запятой

---

Всегда ставь точку с запятой в конце инструкции.

```javascript
let message = "Привет!";
```

## 🔹 Именование

---

Используй _camelCase_ для переменных и функций.

```javascript
let userName = "Алиса";
function getUserName() {
  return userName;
}
```

## 🧹 Дополнительные рекомендации

---

-Избегай длинных строк: старайся, чтобы строки кода не превышали 80 символов.
-Комментируй сложные участки кода: это поможет другим (и тебе в будущем) понять логику.
-Соблюдай единообразие: придерживайся одного стиля во всем проекте.

## 🛠 Инструменты для поддержания стиля

---

**ESLint**: анализатор кода, который помогает выявлять и исправлять проблемы со стилем.
**Prettier**: форматировщик кода, который автоматически приводит код к единому стилю.

## ✅ Краткий итог

---

-Соблюдение стиля кода делает проект более читаемым и поддерживаемым.
-Используй отступы, пробелы и точки с запятой согласно рекомендациям.
-Придерживайся единообразного именования переменных и функций.
-Используй инструменты, такие как ESLint и Prettier, для автоматического контроля стиля.

### 🎯 Следование стилю кода — это шаг к профессионализму и эффективной командной работе.
