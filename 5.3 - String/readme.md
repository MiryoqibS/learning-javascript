# 📘 Тема 5.3 – Строки в JavaScript

## 🧠 Что я узнал?

В этой теме я подробно изучил, как работают строки в JavaScript: как с ними обращаться, какие методы существуют и как правильно обрабатывать текст.

---

## 🔤 Что такое строки?

Строки — это упорядоченные наборы символов, заключённые в:

- Одинарные кавычки: `'текст'`
- Двойные кавычки: `"текст"`
- Обратные кавычки (шаблонные строки): `` `текст` ``

---

## 📌 Основные свойства и особенности:

- Строки неизменяемы (immutable) — любые методы возвращают **новую строку**, не изменяя исходную.
- Строки можно перебирать как массив:

```javascript
for (let char of "Привет") {
  console.log(char); // П, р, и, в, е, т
}
```

---

## 🛠 Полезные методы:

### `.length` Возвращает длину строки:

```javascript
"Якуб".length; // 4
```

### Доступ к символу:

```javascript
let str = "JavaScript";
console.log(str[0]); // "J"
console.log(str.charAt(1)); // "a"
```

### `.toUpperCase()` / `.toLowerCase()`

Преобразует регистр:

```javascript
"hello".toUpperCase(); // "HELLO"
"HELLO".toLowerCase(); // "hello"
```

### `.indexOf(substr, pos)`

Ищет подстроку:

```javascript
"Я люблю JavaScript".indexOf("люб"); // 2
"Я люблю JavaScript".indexOf("нет"); // -1
```

### `.includes()`, `.startsWith()`, `.endsWith()`

Проверка наличия:

```javascript
"Привет".includes("рив"); // true
"Привет".startsWith("При"); // true
"Привет".endsWith("ет"); // true
```

### `.slice(start, end)`

Извлечение подстроки:

```javascript
"Якуб".slice(1, 3); // "ку"
```

### `.substring(start, end)`

Похож на `slice`, но не поддерживает отрицательные индексы.

### `.substr(start, length)`

Извлекает подстроку длиной length начиная с позиции start (устаревший, но работает).

### 🧪 Сравнение строк

Сравнение идёт посимвольно по **Unicode**:

```javascript
console.log("Я" > "А"); // true
```

---

## 🧩 Шаблонные строки (template literals)

Позволяют вставлять переменные прямо в строку:

```javascript
let name = "Якуб";
console.log(`Привет, ${name}!`);
```

---

## ⚠ Особенности:
-Строки неизменяемы: `str[0] = "X"` — не сработает.
-`charAt` безопаснее, чем `str[i]` — не вернёт undefined.
